import 'package:get/get.dart';

class MainBindings extends Bindings {
  @override
  void dependencies() {
    //HERE YOU CAN SET INITIAL BINDINGS
    // initialBinding: MainBindings(),  - add this line on GetMaterialApp
  }
}
